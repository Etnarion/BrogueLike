package model.elements.items.weapons;

import java.awt.*;

public class Gun extends Weapon {
    public Gun(Point position, int damage, int range, int area) {
        super(position, damage, range, area);
    }
}
